---
interface Props {
  id?: string;
  tone?: "paper" | "dark";
  labelledBy?: string;
  class?: string;
}

const {
  id,
  tone = "paper",
  labelledBy,
  class: className,
} = Astro.props;
---

<section
  id={id}
  class:list={[
    "section-frame",
    `section-frame--${tone}`,
    className,
  ]}
  aria-labelledby={labelledBy}
>
  <div class="content-rail section-frame__inner">
    <slot />
  </div>
</section>
