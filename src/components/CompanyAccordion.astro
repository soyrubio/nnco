---
import FaqList from "./FaqList.astro";
import { waysOfWorking } from "@/data/site";

const items = waysOfWorking.map(({ title, description }) => ({
  question: title,
  answer: description,
}));
---

<FaqList items={items} groupName="company-approach" />
