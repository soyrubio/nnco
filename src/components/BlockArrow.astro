---
interface Props {
  direction?: "down" | "left" | "right";
}

const { direction = "right" } = Astro.props;
---

<span
  class:list={["block-arrow", `block-arrow--${direction}`]}
  aria-hidden="true"
></span>
